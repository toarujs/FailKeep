<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FailKeep.Ui.Pages.JailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid RowSpacing="12" Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="监控 Jail" Style="{StaticResource TitleTextBlockStyle}"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <TextBox x:Name="JailNameBox" PlaceholderText="jail 名称，如 rdp" MinWidth="200" Text="rdp"/>
            <Button Content="查询" Click="Query_Click"/>
        </StackPanel>
        <ListView Grid.Row="2" x:Name="WatchList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Ip}" FontFamily="Consolas"/>
                        <TextBlock Opacity="0.7">
                            <Run Text="失败次数: "/><Run Text="{Binding Fails}"/>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
